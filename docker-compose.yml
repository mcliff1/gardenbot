services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    environment:
      - GARDENBOT_DATA_DIR=/app/data
      - OLLAMA_BASE_URL=http://newstage.cliffnet:11434
      - OLLAMA_MODEL=llama3
